let options = {
    title: "Menu",
    width: 100,
    height: 200
};
let {title,width,height} = options;

document.getElementById('main').innerText = `Title: ${title} | Width: ${width} | Height: ${height}`;